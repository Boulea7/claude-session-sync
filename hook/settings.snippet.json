{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "mcp__codexmcp__codex|mcp__gemini__gemini",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .claude/sessions.json ] || (mkdir -p .claude && echo '{\"_schema_version\":\"1.0\",\"_hint\":\"Track SESSION_IDs here. Update after each MCP call.\",\"tasks\":{}}' > .claude/sessions.json); cat .claude/sessions.json",
            "timeout": 3000
          }
        ]
      }
    ]
  }
}
